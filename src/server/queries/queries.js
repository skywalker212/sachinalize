const grounds = require('./grounds');
const innings = require('./innings');
const opposition = require('./opposition');
const result = require('./result');
const runs_range = require('./runs-range');
const runs = require('./runs');
const year = require('./year');

module.exports = {
    grounds,
    innings,
    opposition,
    result,
    runs_range,
    runs,
    year
}